parameters:
- name: 'applications' 
  type: object
- name: 'projectinfo'
  type: object  
  default:
    - project:'additionalTariff'
      deploy:true    
    - project:'b2b'
      deploy:true    

steps:

- script: 'echo building $${{ parameters.applications.tallySheet.deploy }}'

- ${{ each p in parameters.projectinfo }}:
  - script: 'abc echo building $${{ p.project }}'

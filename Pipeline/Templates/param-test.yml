parameters:
- name: 'project' 
  type: object
  default:
    - Web1
    - Web2
- name: 'webApplication' 
  type: string

variables:
- template: ./vars-web.yml
  
steps:
- ${{ each p in parameters.project }}:
  - script: 'echo building $${{ p }}'
  - script: 'echo building $${{ parameters.webApplication }} $(webSiteDeclarationSetting)'
    

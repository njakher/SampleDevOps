parameters:
- name: 'project' 
  type: object
  default:
    - web1
    - web2

steps:
- ${{ each p in parameters.project }}:
  - template: build.yml
    parameters:
      project: '**/${{ p }}.csproj'
      packageName: ${{ p }}